# Bengali Dictionary Seed

This is intentionally small seed data for local MVP development. Production data can be swapped in through the same entry shape and seeded into SQLite.

Each entry is NFC-normalized at seed time by the API package.
